\documentclass{standalone}
\usepackage[dvipsnames]{xcolor}
\usepackage{tikz}
\usepackage{tikz-3dplot}
\usepackage{xifthen}
\usepackage{bm}
\usepackage[outline]{contour}
\usetikzlibrary{3d,calc,patterns, decorations.pathmorphing, decorations.markings}
\usetikzlibrary{arrows}
\usetikzlibrary{positioning,calc}
\renewcommand{\vec}{\bm}
\newcommand{\lsDataPointLetter}{D}
\newcommand{\lsDataPoint}{\vec{\MakeLowercase{\lsDataPointLetter}}}
\newcommand{\lsDataPointMatrix}{\MakeUppercase{\lsDataPointLetter}}
\newcommand{\lsDataPointConcMatrix}{\tilde{\lsDataPointMatrix}}
\newcommand{\lsControlPointLetter}{P}
\newcommand{\lsControlPointMatrix}{\MakeUppercase{\lsControlPointLetter}}
\newcommand{\lsControlPoint}{\vec{\MakeLowercase{\lsControlPointLetter}}}
\newcommand{\lsControlPointCoef}{c}
\newcommand{\lsControlPointCoefVec}{\vec{\lsControlPointCoef}}
\newcommand{\lsControlPointCoefMatrix}{\MakeUppercase{\lsControlPointCoef}}
\newcommand{\lsControlCoefConcMatrix}{\tilde{\lsControlPointCoefMatrix}}
\newcommand{\norm}[1]{\left\| #1 \right\|}

\renewcommand{\vec}{\bm}
\begin{document}
\tdplotsetmaincoords{30}{60}
\begin{tikzpicture}[tdplot_main_coords]
\tdplotsetrotatedcoords{0}{90}{0}

%%% first patch
\coordinate (patchABase) at (0,0,0);

\foreach \i in {0,..., 4}
{
	\foreach \j in {0,..., 4}
	{
		\coordinate(Ao \i \j) at ($(patchABase) + \i*(1,0,0)+\j*(0,1,0)$);
		\coordinate(A \i \j) at ($(Ao \i \j) - {(\i*(\i-4) + \j*(\j-4))}*(0,0,0.25)$);				
	}
}	

\foreach \i in {0.1,0.2,..., 4}
{
	\foreach \j in {0,..., 4}
	{			
		\draw[thick,blue] 
		($(patchABase) + {(\i-0.1)}*(1,0,0)+\j*(0,1,0) - {((\i-0.1)*(\i-0.1-4) + \j*(\j-4))}*(0,0,0.25)$) 
		-- 
		($(patchABase) + \i*(1,0,0)+\j*(0,1,0) - {(\i*(\i-4) + \j*(\j-4))}*(0,0,0.25)$);	
		\draw[very thin] 
		($(patchABase) + {(\i-0.1)}*(1,0,0)+\j*(0,1,0)$) 
		-- 
		($(patchABase) + \i*(1,0,0)+\j*(0,1,0)$);
	}
}

\foreach \i in {0,..., 4}
{
	\foreach \j in {0.1,0.2,..., 4}
	{		
		\draw[thick,blue] 
		($(patchABase) + \i*(1,0,0)+{(\j-0.1)}*(0,1,0) - {(\i*(\i-4) + (\j-0.1)*(\j-0.1-4))}*(0,0,0.25)$) 
		-- 
		($(patchABase) + \i*(1,0,0)+\j*(0,1,0) - {(\i*(\i-4) + \j*(\j-4))}*(0,0,0.25)$);
		\draw[very thin] 
		($(patchABase) + \i*(1,0,0)+{(\j-0.1)}*(0,1,0)$) 
		-- 
		($(patchABase) + \i*(1,0,0)+\j*(0,1,0)$);						
	}
}

\fill[blue,opacity=.3]
(patchABase)
\foreach \i in {0,0.1,..., 4}
{
-- ($(patchABase) + \i*(1,0,0) - {\i*(\i-4)}*(0,0,0.25)$)
}
\foreach \j in {0,0.1,..., 4}
{
-- ($(patchABase) + 4*(1,0,0)+\j*(0,1,0) - {\j*(\j-4)}*(0,0,0.25)$)
}
\foreach \i in {4,3.9,..., 0}
{
-- ($(patchABase) + \i*(1,0,0)+4*(0,1,0) - {\i*(\i-4)}*(0,0,0.25)$)
}
\foreach \j in {4,3.9,..., .1}
{
-- ($(patchABase) + \j*(0,1,0) - {\j*(\j-4)}*(0,0,0.25)$)
};

\contourlength{.15em}
\coordinate(Atrans 11) at ($(Ao 11) +(0.2,0.2,0)- {(1.2(1.2-4) + 1.2*(1.2-4))}*(0,0,0.25)$);
\coordinate(Ap) at ($(Atrans 11)+(0,0,2.3)$);
\draw[dashed,draw=red,thick] (Atrans 11)--(Ap);
\node[circle,fill=red,draw=black, label={[red,xshift=.1cm]left:$\vec{d}_k$},inner sep=2pt] at (Ap){};
\node[circle,fill=blue,draw=black, label={[blue,yshift=-.1cm,xshift=-.1cm]above right:\contour{white}{$\vec{S}\left(u_k,v_k\right)$}},inner sep=2pt] at (Atrans 11){};

%%% second patch
\coordinate[right = 6cm of patchABase] (patchBBase);

\foreach \i in {0,..., 4}
{
	\foreach \j in {0,..., 4}
	{
		\coordinate(Bo \i \j) at ($(patchBBase) + \i*(1,0,0)+\j*(0,1,0)$);
		\coordinate(B \i \j) at ($(Bo \i \j)$);					
	}
}

\foreach \i in {0.1,0.2,..., 4}
{
	\foreach \j in {0,..., 4}
	{			
		\draw[very thin] 
		($(patchBBase) + {(\i-0.1)}*(1,0,0)+\j*(0,1,0)$) 
		-- 
		($(patchBBase) + \i*(1,0,0)+\j*(0,1,0)$);	
	}
}

\foreach \i in {0,..., 4}
{
	\foreach \j in {0.1,0.2,..., 4}
	{		
		\draw[very thin] 
		($(patchBBase) + \i*(1,0,0)+{(\j-0.1)}*(0,1,0)$) 
		-- 
		($(patchBBase) + \i*(1,0,0)+\j*(0,1,0)$);	
	}
}

\foreach \i in {1,1.5,2}
{
	\foreach \j in {1,1.5,2}
	{
	\node[circle, fill=OliveGreen, draw=black,inner sep=2pt] at ($(patchBBase) + \i*(1,0,0)+\j*(0,1,0)$){};
	}
}
\contourlength{.15em}
\node[label={[OliveGreen,yshift=.1cm,xshift=-.1cm]below right:\contour{white}{$\vec{B}_{i,j}^{Bez}$}}] at ($(patchBBase) + 2*(1,0,0)+1.5*(0,1,0)$){};


%%% third patch
\coordinate[right = 6cm of patchBBase] (patchCBase);

\foreach \i in {0,..., 4}
{
	\foreach \j in {0,..., 4}
	{
		\coordinate(Co \i \j) at ($(patchCBase) + \i*(1,0,0)+\j*(0,1,0)$);
		\coordinate(C \i \j) at ($(Co \i \j)$);						
	}
}

\foreach \i in {0.1,0.2,..., 4}
{
	\foreach \j in {0,..., 4}
	{			
		\draw[very thin] 
		($(patchCBase) + {(\i-0.1)}*(1,0,0)+\j*(0,1,0)$) 
		-- 
		($(patchCBase) + \i*(1,0,0)+\j*(0,1,0)$);	
	}
}

\foreach \i in {0,..., 4}
{
	\foreach \j in {0.1,0.2,..., 4}
	{		
		\draw[very thin] 
		($(patchCBase) + \i*(1,0,0)+{(\j-0.1)}*(0,1,0)$) 
		-- 
		($(patchCBase) + \i*(1,0,0)+\j*(0,1,0)$);	
	}
}

\foreach \i in {0.5,1.5,2.5}
{
	\foreach \j in {0.5,1.5,2.5}
	{
	\node[circle, fill=orange, draw=black,inner sep=2pt] at ($(patchCBase) + \i*(1,0,0)+\j*(0,1,0)$){};
	}
}

\foreach \i in {0,4}
{
	\foreach \j in {0,4}
	{
\node[circle, fill=Gray, draw=black,inner sep=2pt] at (C \i \j){};
	}
}
\contourlength{.15em}
\node[label={[Gray,yshift=.1cm]below:\contour{white}{$\vec{m_k}\in M$}}] at (C 44){};
\node[label={[orange]right:\contour{white}{$\vec{B}_{i,j}^{PS}\in V_x$}}] at (C 31){};

%%% arrows and stuff
\draw[->,>=stealth',auto]
($(A 04)+(0:.5cm)$) 
to [out=30,in=150] 
node[align=center]{Bernstein polynomials \\ give coefficients $C\left(u_k,v_k\right)$} 
($(B 04)+(180:.5cm)$);

\draw[->,>=stealth',auto]
($(B 04)+(0:.5cm)$) 
to [out=30,in=150] 
node[align=center]{Peters' scheme \\ gives coefficients $C^{PS}$}
($(C 04)+(180:.5cm)$);

\draw[->,>=stealth',auto]
($(C 40)+(180:.5cm)$) 
to [out=210,in=-30] 
node[align=center]{$\sum \left[C^{PS}\right]_{i,j}P_{i,j}$ \\ gives control points $\bm{p}_{i,j}$} 
($(B 40)+(0:.5cm)$);

\draw[->,>=stealth',auto]
($(B 40)+(180:.5cm)$) 
to [out=210,in=-30] 
node[align=center]{$\sum\left[C\left(u_k,v_k\right)\right]_{i,j}\bm{p}_{i,j} $ \\ gives surface point $\vec{S}\left(u_k,v_k\right)$} 
($(A 40)+(0:.5cm)$);

%%% loop arrow 
\draw [->,>=stealth',auto]
($(C 04)+(0:.5cm)$)
to [out=30,in=-30,looseness=3]
node[pos=0.125,align=center]{Least squares fitting \\ $ \norm{\lsDataPointMatrix - \lsControlPointCoefMatrix(\vec{u},\vec{v})\lsControlPointMatrix}^2$}
($(C 40)+(0:.5cm)$);

\end{tikzpicture}

\end{document}